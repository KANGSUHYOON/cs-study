# 조건식을 간략하게 줄여보자

## 예시(example)
```cpp
int main() {
  cin >> A >> B;

  if (A > B) {
    cout << ">";
  }
  else if (A < B) {
    cout << "<";
  }
  else {
    cout << "==";
  }
}
```

## 삼항연산자 (Conditional Operator) 사용법
```cpp
(조건식) ? [실행코드 A] : [실행코드 B]
```
> '조건식'이 참일 경우 A를 반환하고, 거짓일 경우 B를 반환한다.

 만일 if, else if, else처럼 3개가 있는경우는 어떻게 할까?
 ```cpp
 (조건식1) ? [실행코드 A] : ((조건식 2) ? [실행코드 B] : [실행코드 C])
 ```

## 이제 실사용을 해보자
```cpp
int main() {
  cin >> A >> B;

  cout << ((A > B) ? ">" : ((A < B) ? "<" : "=="));
}
```
그 길던 코드가 두줄로 끝나버렸다.

## 🚫남발은 하지말자
- 조건이 3개 이상인데 각각의 의미가 복잡할 때 쓰면 가독성이 떨어진다
- 조건이 2개여도 실행 코드가 길면 가독성이 떨어진다
- 조건이 3개 이상부터면 if/else를 쓰는 것이 더 낫다

### 🔖참고 자료
// Reference: https://st-lab.tistory.com/217
  
