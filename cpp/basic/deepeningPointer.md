# 😃포인터를 실전에서 쓸 수 있게!

## 포인터 한 줄 정의
### 📫포인터(pointer) = "값이 아니라 주소를 저장하는 변수"
- `int x = 10;` -> x는 "10"이라는 값을 담고 있음
- `int* p = &x; -> p는 "x가 메모리에 있는 위치(주소)"를 담고 있음

## 📬주소(&) / 📭역참조(*)
1) 주소 연산자 `&`
```cpp
int x = 10;
int* p = &x; //p는 x의 주소를 갖는다
```
2) 역참조 연산자 `*`(p가 가리키는 곳의 "값")
```cpp
cout << *p;    // 10 출력
*p = 20;       // x를 20으로 바꿈
cout << x;     // 20
```
**🙏기억하기**
- `&x` : x의 "주소"
- `*p` : p가 가리키는 "값"

## 🫂배열과 포인터는 공존관계
### 배열 이름은 "첫 원소의 주소"처럼 동작한다
```cpp
int A[5] = {1,2,3,4,5};

cout << A;      // 첫 원소의 주소 같은 느낌이다
cout << &A[0];  // 거의 같은 의미이다
```
### 그럼 이것도 된다
```cpp
cout << A[2];      // 3
cout << *(A + 2);  // 3(같은 의미이다)
```
핵심 공식:
> `A[i] == *(A + i)`
이게 **이진탐색, 정렬, std::find 같은 것들이 "포인터 범위"로 동작하는 이유**이다.

## 🚶포인터 연산(주소 이동) - "칸 단위"로 움직인다
```cpp
int A[3] = {10, 20, 30};
int* p = A;          // p = &A[0]

p = p + 1;           // 다음 원소로 이동 (int 한 칸)
cout << *p;          // 20
```
오해 방지:
> - `p + 1`은 주소에 1 더하는게 아니라
> - **자료형 크기만큼 이동**한다
> - - `int*`면 보통은 4BYTE씩

## 동적 할당(new/delete)
ex) BOJ 1920
```cpp
int* A = new int[N];
...
delete[] A;
```
1) 왜 쓸까?
- `int A[100000];` 처럼 **컴파일 타임 크기**가 확정되면 편하다
- 그러나 N이 입력으로 들어와서 "얼마나 큰 배열인지 런타임에 결정"되면 <br>
  -> `new int[N]`이 필요할 수 있다

2) 문법 핵심
- `new int[N]` -> int가 N개 연속으로 있는 공간을 만들고, **첫 칸 주소를 반환**
- 그래서 타입이 `int*` 이다

3) 해제 규칙
- `new int[N]`로 만들었으면 **반드시 `delete[] A;**
- `new int`로 한 개 만들었으면 `delete p;`

## 🎁포인터로 범위 전달 (sort / binary_search)
C++ 표준 알고리즘은 배열의 범위를 다음과 같이 받는다. 
> `(begin, end)`(end는 포함x)
따라서
```cpp
sort(A, A + N);
binary_search(A, A + N, x);
find(A, A + N, x);
```
- `A` : 첫 원소 주소(begin)
- `A + N` : 마지막 다음 주소(end)

## 💡포인터 팁
1. 함수에서 배열 받기
```cpp
void function(int* A, int N) {...}
// OR
void function(int A[], int N {...} // 사실상 똑같다
```
2. 배열 길이는 포인터만으로는 모른다
- `int* A`만 있으면 몇 칸인지 모름 -> 그래서 N을 같이 넘김
3. 배열 복사는 포인터로는 자동으로 안 된다
- 포인터는 "주소"라서 `B = A;`하면 값 복사가 아니라 **같은 곳을 가리킨다**
4. nullptr은 **아무것도 안 가리킨다**
```cpp
int* p = nullptrl;
```
- 안전한 초기화 방법이다
