## 📌 포인터와 배열의 관계

### ❓ 왜 배열은 함수로 전달하면 포인터처럼 동작할까?

C/C++에서 **배열 이름은 배열 자체가 아니라, 배열의 첫 번째 원소의 주소**를 의미한다.  
따라서 배열을 함수로 전달하면 **값 복사가 아닌 주소 전달**이 일어난다.

---

### 🔹 배열 전달 예시
```cpp
void func(char s[]) {
    s[0] = 'A';
}
```
위 함수 선언은 실제로 아래와 **완전히 동일하게 동작한다.**
```cpp
void func(char* s) {
    s[0] = 'A';
}
```
📌즉, `char s[]`는 **배열을 받는 것이 아니라** <br>
**배열의 시작 주소를 받는 포인터 변수**이다.

## 🔷실제 사용 예시
```cpp
char arr[] = "apple";
func(arr);
```
실행 결과:
```
"A"pple
```
함수 내부에서 수정한 내용이 **원본 배열(arr)에 그대로 반영된다.**

## 🧠 메모리 관점에서 이해하기
```
main 함수:
arr  →  [ a ][ p ][ p ][ l ][ e ][ \0 ]

func 함수:
s    →  [ a ][ p ][ p ][ l ][ e ][ \0 ]
```
- `arr`과 `s`는 이름만 다를 뿐 **같은 메모리 공간을 가리킨다.**

## ⚠️ 주의점

함수 내부에서 배열의 **크기 정보는 알 수 없다.**
```cpp
sizeof(s);
```
`sizeof(s)`는 
- 배열 크기가 아니라 **포인터 크기(보통은 8byte)** 를 반환한다.
그래서 배열을 함수로 전달할 때는
- 1. 길이를 함께 전달
  2. 문자열의 경우 '\0'에 의존

## ✔️ 핵심 요약
- 배열 이름은 첫 원소의 주소
- `func(char s[])` = `func(char* s)`
- 함수로 전달 시 **복사 x, 주소 전달 👌**
- 함수 안에서는 배열 크기를 알 수 없음
