# BOJ 2052(remainder) 

## 📖문제 요약
수 10개를 입력받은 뒤, 이를 42로 나눈 나머지를 구한다.<br>
그 다음 서로 다른 값이 몇개 있는지 출력하는 프로그램을 작성하라.

## 🫂접근 방법
- 42로 나눈 나머지를 구한다 -> 크기 42짜리 배열을 생성한다.
- 배열 값은 저장 전에 0으로 초기화한다.
- 인덱스로 관리
- 만약 봤던 값이라면 (그 인덱스의 값이 1이라면) 계속 1로 저장.

## ⌨️CODE
```cpp
#include <iostream>
using namespace std;

int main() {
	int input;
	int check[42] = { 0 };
	for (int i = 0; i < 10; i++) {
		cin >> input;
		check[input % 42] = 1;
	}
	int count = 0;
	for (int i = 0; i < 42; i++) {
		if (check[i] == 1) {
			count++;
		}
	}
	cout << count << endl;
}
```

## 💡포인트
- **나머지 값을 인덱스로 사용해 중복을 제거**
