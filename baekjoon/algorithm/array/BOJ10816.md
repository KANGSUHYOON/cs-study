# 백준 10816 - 숫자 카드 2

## 📖문제 요약
- N개의 숫자 카드가 주어지고,
- M개의 숫자에 대해 각각 **몇 장이 있는지(개수)** 출력한다.

## 💡아이디어
1. 카드 배열을 정렬한다.
2. 각 쿼리 값 `x`에 대해
   - `lower_bound`로 `x`의 시작위치
   - `upper_bound`로 `x`의 끝 다음 위치를 구한다.
3. 두 위치 차이를 출력하면 `x`의 개수가 된다.

## ⏲️시간 복잡도
- 정렬: `O(N log N)`
- 각 쿼리 탐색: `O(log N)` × M
  -> 전체: `O(N log N + M log N)`

## 🌟핵심 코드
```cpp
int cnt = upper_bound(A.begin(), A.end(), x) - lower_bound(A.begin(), A.end(), x);
cout << cnt << ' ';
```

## ⌨️전체 코드
```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    cin >> N;
    vector<int> A(N);
    for (int i = 0; i < N; i++) cin >> A[i];

    sort(A.begin(), A.end());

    int M;
    cin >> M;
    for (int i = 0; i < M; i++) {
        int x; cin >> x;
        int cnt = upper_bound(A.begin(), A.end(), x) - lower_bound(A.begin(), A.end(), x);
        cout << cnt << ' ';
    }
    return 0;
}
```

## ❓느낀 점
- 1920번은 `binary_search`로 존재 여부만 확인했다면,
- 10816번은 `lower_bound/upper_bound`로 **같은 값이 연속인 구간 길이**를 구해 개수를 계산했다.
