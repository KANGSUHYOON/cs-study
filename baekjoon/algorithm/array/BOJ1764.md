# BOJ 1764 정리

## 📖문제 요약
- 듣도 못한 사람 N명
- 보도 못한 사람 M명
- **두 그룹에 모두 속한 사람의 이름을 사전순으로 출려**

## 💡아이디어
- 듣도 못한 사람을 `vector<string>`에 저장
- **정렬 후 이분 탐색**으로 보도 못한 사람을 하나씩 검사
- 존재하면 결과 벡터에 저장
- 결과를 다시 정렬 후 출력

💠핵심 조건
> **이분 탐색은 반드시 정렬된 자료구조에서만 사용 가능**

## 🧰사용한 도구들
### vector<string>
- 문자열 여러 개를 저장하기에 적합
- 크기 가변 -> 입력 개수 유동적일 때 유리

### sort + binary_search
```cpp
sort(A.begin(), A.end());
bianry_search(A.begin(), A.end(), name);
```
- `binary_search`는 **존재 여부(true / false)** 만 판단
- 개수는 세지 않음
- 시간 복잡도:
  - 정렬: O(N log N)
  - 탐색: O(log N)

### 결과 저장용 vector
```cpp
vector<string> result;
```
- `cnt++`대신 **이름 자체를 저장**
- 출력 요구사항(사전순)을 만족하기 위해 마지막에 정렬

## 📖range-based for 문 이해
```cpp
for (const string& s : result) {
  cout << s << '\n';
}
```
### 🔶 의미하는 바
- `result`안에 있는 문자열을 **하나씩 꺼내서**
- `s`라는 이름으로 **참조(reference)** 해서 사용

### ♦️ 왜 참조(&)를 쓸까?
- 문자열 복사를 방지 (성능)
- 원본 데이터를 그대로 사용

### ♦️ 왜 const를 붙이는가?
- 출력만 하므로 **수정 방지**
- 의도 명확 + 실수 방지

➡️ 내부적으로는 거의 아래 코드와 동일하다
```cpp
for (int i = 0; i < result.size(); i++) {
  const string& s = result[i];
  cout << s << '\n';
}
```

## string / 참조 / const 정리
| 형태                | 의미                   |
| ----------------- | -------------------- |
| `string s`        | 문자열 **복사**           |
| `string& s`       | 원본 문자열의 **별명(참조)**   |
| `const string& s` | 원본 문자열을 **읽기 전용 참조** |

알고리즘에서는
> **읽기만 하면 `const T&`가 기본 패턴이다**


## ❓reserve(min(N, M))은 뭘까?
- `vector`가 **미리 메모리 공간을 확보**하도록 지시하는 코드
### 왜 쓸까?
`vector`는 push_back 할 때:
- 공간 부족 -> **새 메모리 할당**
- 기존 요소 전부 복사 -> 비용 발생
`reserve()`를 쓰면:
- **미리 충분한 공간 확보**
- push_back 시 재할당 방지

### 왜 `min(N, M)`?
- 교집합 최대 개수는 **둘 중 작은 쪽**
- 불필요하게 큰 메모리 안 잡음

✏️ 필수는 아니지만 **성능 습관 + 실무 감각**으로 매우 좋다

## ⌨️code
```cpp
#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N, M;
    cin >> N >> M;

    vector<string> A(N);
    for (int i = 0; i < N; i++) {
        cin >> A[i];
    }

    sort(A.begin(), A.end());

    vector<string> result;

    for (int i = 0; i < M; i++) {
        string name;
        cin >> name;

        if (binary_search(A.begin(), A.end(), name)) {
            result.push_back(name); 
        }
    }

    sort(result.begin(), result.end()); 

    cout << result.size() << '\n';
    for (const string& s : result) {
        cout << s << '\n';
    }

    return 0;
}
```

## ⭐오늘의 핵심 한 줄
> 참조는 복사가 아니라 **같은 객체를 다른 이름으로 부르는 것**
